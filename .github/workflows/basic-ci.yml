name: Basic CI

on:
  push:
  pull_request:

jobs:
  basic-check:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Simple validation
        run: |
          echo "Running basic CI check"
          echo "All good"
